<a
	href="https://topmate.io/akarshanarora/1684042"
	target="_blank"
	rel="noopener"
	class="bundle-card"
>
	<span class="bundle-eyebrow">Companion bundle · 5 PDFs · Free</span>
	<span class="bundle-title">Get the full VLSI bundle — for free</span>
	<span class="bundle-desc">
		The complete <em>Mastering YAML for Semiconductor</em> book this site is built from, plus 4
		Python-for-VLSI guides: CLI Python, common pitfalls, plotting, and interview prep.
	</span>
	<span class="bundle-cta">Download all 5 PDFs →</span>
</a>

<style>
	.bundle-card {
		display: flex;
		flex-direction: column;
		gap: 0.45rem;
		margin: 3rem 0 0;
		padding: 1.5rem 1.75rem;
		border: 1px solid var(--color-accent);
		border-radius: 12px;
		background: linear-gradient(135deg, #fff5ec 0%, #fbf6ee 100%);
		text-decoration: none !important;
		color: inherit;
		transition: all 0.25s ease;
	}
	.bundle-card:hover {
		transform: translateY(-2px);
		box-shadow: 0 6px 18px rgba(196, 93, 44, 0.18);
	}
	.bundle-eyebrow {
		font-family: var(--font-ui);
		font-size: 0.7rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		color: var(--color-accent);
	}
	.bundle-title {
		font-size: 1.15rem;
		font-weight: 700;
		color: var(--color-text-primary);
		font-family: var(--font-body);
	}
	.bundle-desc {
		font-size: 0.88rem;
		line-height: 1.55;
		color: var(--color-text-secondary);
		font-family: var(--font-body);
	}
	.bundle-desc em {
		font-style: italic;
	}
	.bundle-cta {
		margin-top: 0.35rem;
		font-family: var(--font-ui);
		font-size: 0.82rem;
		font-weight: 600;
		color: var(--color-accent);
	}
</style>
